let numA = 8;
let numB = 32;
let cociente; 

if (numB === 0) {
    console.log("la division no es posible. El srgundo numero (B) es igual a 0 ") ;
} else {
    let cociente = numA / numB ;
    console.log ("El cociente entre " +numA + " y " + numB + "es :" + cociente );
}